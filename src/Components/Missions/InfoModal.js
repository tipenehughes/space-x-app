import React from "react";
import styles from "../../CSS/InfoModal.module.css";

const InfoModal = ({ data }) => {
    console.log(data);
    return <div className={styles.infoModal}></div>;
};

export default InfoModal;
